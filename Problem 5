#include <iostream>

using namespace std;

int main()
{
    int x=21;

    while(x>0)
    {
        for(int a=2;a<21;a++)
        {
            if(x%a!=0)
            break;

            else if(a==20&&x%a==0)
            {cout<<x;
            x=-1;
            break;}

        }
    x++;
    }
}
